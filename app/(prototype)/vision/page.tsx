import VisionPage from '@/components/prototype/VisionPage';
import { metadataForPage } from '@/lib/seo';

export const metadata = metadataForPage('vision');

export default function VisionRoutePage() {
  return <VisionPage />;
}
